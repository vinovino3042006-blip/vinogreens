create database company_db;

use company_db;

create table skill(skill_id int identity(1,1) primary key,skill_name varchar(20),category varchar(20));

create table employee(empid int,empname varchar(20));
select *from employee;


// data base la erukura table ella show agum
select name from sys.databases;
or
exec sp_databases;


// table name show agum
select name from sys.tables;
or
SELECT * FROM INFORMATION_SCHEMA.TABLES;

exec sp_rename 'employee','staff';

alter table staff add email varchar(20),databirth date, salary int,gender varchar(20);

select * from staff;

insert into staff values(1,'Alice Green','a.green@company.com','2024-01-10',62000.00,'Female');

create table project_table(department_id int,program varchar(30),budget int);

insert into project_table values( 1,'Training Program',25000.00);

select * from project_table;
insert into project_table values( 2,'Mobile App',60000.00);

create table department(dept_name varchar(20),location varchar(20));
insert into department(dept_name,location) values ('sales','boston');
insert into department values ('account','manager');
select * from department;

insert into staff(first_name,email) values('tom','tom@company');

alter table staff add first_name varchar(20);

select * from staff;

delete from staff where empname='tom';

ALTER TABLE staff DROP COLUMN empname;

select  empid as Employee_ID,first_name as name ,salary ,email as Email_Address from staff;

select * from employees where hire_date > '2023-01-01';

select * from project where budget > 40000.00 order by budget desc;

select distinct(location) from department;


create table employee(id int identity(1,1),department varchar(30),name varchar(20),salary int , gender varchar(10),address text);
select * from employee;
insert into employee values('IT','njdg',760000,'male','channaijhdbfad','bhh@hvu43454' );
drop table employee;
alter table employee add phone_number varchar(15), email varchar(50);

update employee set salary=65000 where name='john deo';

update employee set gender='other' where department='IT';

alter table employee drop column phone_number;

select * from employee where salary between 60000 and 80000;

select * from employee where name like 'j%';

create table project(dep_id int identity(1,1),department varchar(30),name varchar(20),salary int , gender varchar(10),address text);
select * from project;
drop table project;
insert into project values('ece','njdg',760000,'male','channaijhdbfad' );

select * from project where dep_id in(1,2);

select * from employee where email is not null;

select * from department where location not in('new york','chicago');

select sum(salary)as total_salary from employee;

select avg(budget)as average_budget from project_table;

select max(salary)as highestsalary from employee;

select count(id) as total_emp from employee;

select min(budget)as minimum_budget from project_table;

select * from employee;
select * from department;
 
select e.name,d.department from employee e join department d on e.dep_id=d.dep_id;

select e.department,count(e.emp-id)as totel_employee from departement d left join employee e on d.dep_id=e.dep_id group by d.dept_name;

 select p.project_name,d.department_name from project p join department d on p.dep_id=d.dep_id;

 select e.* from employee e join department d on e.dep_id=d.dep_id where d.location='san francisco';

 select d.department_name from department d left join project p on d.dep_id=p.dep-id where p.project_id is null;

 select concat(first_name,' ',last_name)as fill_name from employee;

 select upper(dept_name) from department;

 select substring(email,1,3)as first_name from employee;

 select abs(-50000);

 select round(avg(cast(salary as decimal(10,2))),2) from employee;
 select round(avg(salary),2) from employee;

 SELECT ROUND(AVG(salary), 2) AS avg_salary
FROM employees;

SELECT * FROM employees ORDER BY emp_id LIMIT 3 OFFSET 3;

SELECT emp_id, first_name, salary, IF(salary >= 70000, 'High', 'Low') AS salary_category FROM employees;

SELECT project_id, budget,
       CASE
           WHEN budget >= 60000 THEN 'Large'
           WHEN budget >= 40000 THEN 'Medium'
           ELSE 'Small'
       END AS budget_category
FROM projects;

SELECT dept_id, SUM(budget) AS total_budget
FROM projects
GROUP BY dept_id;

SELECT * FROM employees ORDER BY LENGTH(first_name) DESC LIMIT 1;

SELECT * FROM employees WHERE hire_date >= DATE_SUB('2025-03-22', INTERVAL 90 DAY);

DELETE FROM employees WHERE salary < 60000;

DROP TABLE projects;

mysql -u username -p database_name < backup.sql
SHOW TABLES;
SELECT table_name, data_length, index_length
FROM information_schema.tables
WHERE table_schema = 'database_name';
DROP DATABASE database_name;
